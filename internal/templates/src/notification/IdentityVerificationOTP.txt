This email has been sent to you in order to validate your identity. Purpose: {{ .Title }}.

If you did not initiate the process your credentials might have been compromised and you should:
	1. Reset your password or other login credentials
	2. Visit the revocation link
	3. Contact an Administrator

To confirm your identity please use the following single-use code: {{ .OneTimeCode }}

This email was generated by a user with the IP {{ .RemoteIP }}.

The following link can be utilized to revoke the code (this is logged event): {{ .RevocationURL }}
